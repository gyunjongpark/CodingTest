#include<iostream>
using namespace std;
typedef unsigned long long ll;

ll a, b, c;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    cin >> a >> b >> c;
    cout << a + b + c;

    return 0;
}
